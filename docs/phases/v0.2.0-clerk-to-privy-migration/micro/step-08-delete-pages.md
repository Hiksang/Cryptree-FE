# Step 08: Sign-in/Sign-up 페이지 삭제

## 메타데이터
- **난이도**: 🟢 쉬움
- **롤백 가능**: ✅ (git restore)
- **선행 조건**: Step 07

---

## 1. 구현 내용 (design.md: P16-P17)
- `src/app/sign-in/[[...sign-in]]/page.tsx` — 삭제
- `src/app/sign-up/[[...sign-up]]/page.tsx` — 삭제
- 디렉토리 전체 삭제

## 2. 완료 조건
- [x] `src/app/sign-in/` 디렉토리 미존재
- [x] `src/app/sign-up/` 디렉토리 미존재
- [x] `npm run build`에서 해당 라우트 미등록

---

## Scope

### 삭제 대상

| 경로 | 설명 |
|------|------|
| `src/app/sign-in/[[...sign-in]]/page.tsx` | Clerk `<SignIn />` 래퍼 페이지 (63줄) |
| `src/app/sign-up/[[...sign-up]]/page.tsx` | Clerk `<SignUp />` 래퍼 페이지 (66줄) |

### 삭제 근거
- Privy는 모달 전용 UI → 별도 페이지 불필요
- Step 07에서 모든 로그인 링크를 `login()` 호출로 교체 완료
- Step 05 미들웨어에서 `/?login=required`로 리다이렉트 (기존 `/sign-in` 리다이렉트 대체)

### Side Effect 위험
- 외부에서 `/sign-in` 직접 접근 시 404 → `/?login=required` 리다이렉트로 대체됨 (미들웨어에서 처리)

## FP/FN 검증

### False Positive: 0건
### False Negative: 0건

### 검증 통과: ✅

---

→ 다음: [Step 09: 웹훅 마이그레이션](step-09-webhook.md)
